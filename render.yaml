services:
  - type: web
    name: autocoder-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 -m uvicorn api.server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false
